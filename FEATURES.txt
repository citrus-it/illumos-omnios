Features
========

This file documents feature changes at a very high level.

A - means that the support for that feature was removed, a + means that the
support for that feature was added, and a * means that the behavior was
changed.

<next> release
--------------
 - Trusted Solaris aka. Trusted Extensions
 * readdir_r is now POSIX-only version
