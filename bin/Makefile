SUBDIR=	awk banner cat clear dis echo env false grep id infocmp less \
	localedef mandoc make nc openssl pax puname pwd sed size stat tabs \
	tcpdump tic time toe tput true tset tsort tzselect uudecode uuencode \
	uadmin uname utmp_update vtfontcvt xinstall yacc yes zic

.include <subdir.mk>
