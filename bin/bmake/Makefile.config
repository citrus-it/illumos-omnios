CPPFLAGS+=	-I${.CURDIR}/missing
MANTARGET?=	man
# we don't ship a bsd-compatible install(1) at present
INSTALL?=	${.CURDIR}/install-sh
# this is already set in sys/illumos.mk, but needed for SunOS
STRIP_FLAG?=
